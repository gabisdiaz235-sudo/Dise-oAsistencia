<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SistemaGestionAsistencia.Vistas.VistaReporteUsuario"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SistemaGestionAsistencia.Vistas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    xmlns:locall="using:SistemaGestionAsistencia.Servicio_web"
    xmlns:viewmodels="using:SistemaGestionAsistencia.VistaModelo"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <UserControl.Resources>
        <viewmodels:ReporteUsuarioViewModel x:Key="users"/>
        <locall:TimeFormatConverter x:Key="TimeFormatConverter" />
    </UserControl.Resources>



    <Grid CornerRadius="20" BorderBrush="#143446" BorderThickness="2" Background="#143446" Margin="20"
          DataContext="{Binding Source={StaticResource users}}">
        <ScrollViewer Margin="20" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">

            <StackPanel CornerRadius="10" Margin="10" >

                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Margin="5" Foreground="White" Command="{Binding RegresarCommand}">
                            <SymbolIcon Symbol="Back" />
                        </Button>
                        <TextBlock Text="Atras" Margin="5" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock x:Name="idText" Text="" Margin="5" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel>
                        
                        <!--Línea de división-->
                        <TextBlock Text="Datos de los Usuarios" Margin="0 30 10 0" HorizontalAlignment="Center" Foreground="White" FontFamily="Times New Roman" FontSize="20" FontWeight="Bold"/>
                        <Border Height="2" Background="#F6BD3D" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Grid>
                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Fila para la barra horizontal -->
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="10"  HorizontalAlignment="Center" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <!-- Columna para el primer texto -->
                        </Grid.ColumnDefinitions>
                        

                    </Grid>
                </Grid>

                <!--Área de datos generales de un nuevo Usuario-->
                <Grid>
                    <StackPanel>
                        <Grid>
                            <!-- Columnas Para los datos-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="1" Grid.Row="1" Margin="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <!-- Contenedor de campos 1ra columna-->
                                    <StackPanel Grid.Column="1" Margin="5">

                                        <TextBox x:Name="txtIDU" PlaceholderText="ID" Margin="0 0 0 10"  
                                                 Text="{Binding Dato.EmpleadoID}" BorderBrush="#143446" Width="100" HorizontalAlignment="Left"/>
                                        <TextBox x:Name="txtNomU" PlaceholderText="Nombre" Margin="0 0 0 10" BorderBrush="#143446"
                                                 Text="{Binding Dato.Nombre}"/>
                                        <TextBox x:Name="txtApelliU" PlaceholderText="Apellido Paterno" Margin="0 0 0 10" BorderBrush="#143446"
                                                 Text="{Binding Dato.ApellidoPaterno}"/>

                                    </StackPanel>

                                    <!-- Contenedor de campos 2da columna-->
                                    <StackPanel Grid.Column="2" Margin="5">


                                        <TextBox x:Name="txtApelliUm" PlaceholderText="Apellido Materno" Margin="0 43 0 10" BorderBrush="#143446"
                                                 Text="{Binding Dato.ApellidoMaterno}"/>

                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Grid>





                <!--Área de datos de las coordenadas-->
                <Grid>
                    <StackPanel>
                        <Grid>
                            <!-- Columnas Para los datos-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="1" Grid.Row="1" Margin="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <!-- Contenedor de campos 1ra columna-->
                                    <StackPanel Grid.Row="1" >

                                    </StackPanel>

                                    <!-- Contenedor de campos 2da columna-->
                                    <StackPanel Grid.Column="2">

                                        <Button Content="Todos los Reportes" Background="#F6BD3D" Width="200" Height="45" FontSize="20" FontFamily="Times New Roman" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Right"/>

                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Grid>


                <StackPanel CornerRadius="10" Margin="10" >

                    <Grid>
                        <StackPanel>
                            <!--Línea de división y titulo -->
                            <TextBlock Text="Reporte de los Usuario" Margin="0 30 10 0" HorizontalAlignment="Center" Foreground="White" FontFamily="Times New Roman" FontSize="20" FontWeight="Bold"/>
                            <Border Height="2" Background="#F6BD3D" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Grid>







                    <Border Background="#F6BD3D" Width="70" Height="45" CornerRadius="0 0 10 10" HorizontalAlignment="Right" Margin="1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Icono -->
                            <SymbolIcon Symbol="Filter" Foreground="Black" Margin="5"/>

                            <!-- ComboBox con tres opciones -->
                            <ComboBox x:Name="cmbFiltroC" PlaceholderText="" SelectedIndex="-1" Background="Transparent" Width="35">
                                <ComboBoxItem Content="Reportes semanal" />
                                <ComboBoxItem Content="Reportes mensual" />
                                <ComboBoxItem Content="Todos los reportes" />
                            </ComboBox>
                        </StackPanel>
                    </Border>






                    <!-- Datos recabados -->

                    <Grid Margin="2" >

                        <StackPanel>

                            <Grid HorizontalAlignment="Center" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="1" Background="#F6BD3D" Margin="12" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                    <controls:DataGrid 
                                     x:Name="dataGrid"
                                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                                     HorizontalScrollBarVisibility="Visible"
                                     VerticalScrollBarVisibility="Visible"
                                     AlternatingRowBackground="Transparent"
                                     AlternatingRowForeground="SlateGray"
                                     AreRowDetailsFrozen="False"
                                     AreRowGroupHeadersFrozen="True"
                                     AutoGenerateColumns="False"
                                     CanUserSortColumns="False"
                                     CanUserReorderColumns="True"
                                     CanUserResizeColumns="True"
                                     ColumnHeaderHeight="32"
                                     MaxColumnWidth="400"
                                     FrozenColumnCount="0"
                                     GridLinesVisibility="Horizontal"
                                     HeadersVisibility="Column"
                                     IsReadOnly="False"
                                     RowDetailsVisibilityMode="Collapsed"
                                     SelectionMode="Extended"
                                     ItemsSource="{Binding DatosES}"
                                     RowGroupHeaderPropertyNameAlternative="Range">

                                        <controls:DataGrid.Columns>
                                            <controls:DataGridTemplateColumn Header="Fecha">
                                                <controls:DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding HoraEntrada}" TextTrimming="CharacterEllipsis" MaxWidth="95" />
                                                    </DataTemplate>
                                                </controls:DataGridTemplateColumn.CellTemplate>
                                            </controls:DataGridTemplateColumn>
                                            <controls:DataGridTemplateColumn Header="Hora Entrada">
                                                <controls:DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding HoraEntrada.TimeOfDay}" TextTrimming="CharacterEllipsis" MaxWidth="75" />
                                                    </DataTemplate>
                                                </controls:DataGridTemplateColumn.CellTemplate>
                                            </controls:DataGridTemplateColumn>
                                            <controls:DataGridTemplateColumn Header="Hora Salida">
                                                <controls:DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding HoraSalida.TimeOfDay}" TextTrimming="CharacterEllipsis" MaxWidth="75" />
                                                    </DataTemplate>
                                                </controls:DataGridTemplateColumn.CellTemplate>
                                            </controls:DataGridTemplateColumn>
                                            <controls:DataGridTextColumn Header="Latitud" Binding="{Binding Latitud}" Tag="LatitudU" />
                                            <controls:DataGridTextColumn Header="Longitud" Binding="{Binding Longitud}" Tag="LongitudU" />
                                        </controls:DataGrid.Columns>
                                    </controls:DataGrid>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>


                    <!-- Flechas de Siguiente y regresar -->

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <SymbolIcon Symbol="Back" Margin="5" Foreground="White"/>
                        <TextBlock Text="Siguiente" Margin="5" Foreground="White"/>
                        <SymbolIcon Symbol="Forward" Margin="5" Foreground="White"/>
                    </StackPanel>




                    <!-- Nota la libreria en la parte de arriba      xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"     es la que se encarga de generar las tablas, ten mucho cuidado-->

                </StackPanel>




            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
